	<%= render "partials/nav" %>

	<div class="container-fluid">
		<div class="row center-xs">
			<div class="col-lg-6">
				<div id="results-container">
					<% @search.users.each do |user|  %>
					<%= render user unless user == current_user%>
					<% end %>
				</div>
			</div>
			<div class="col-lg-3 space-left">
				<%= render "partials/meta" %>
			</div>
		</div>
	</div>