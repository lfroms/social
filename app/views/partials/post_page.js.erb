$('#feed').append('<%= j render @posts %>');
$(".liked").addClass("exit");
<% if @posts.next_page %>
$('.pagination').replaceWith('<%= j will_paginate @posts %>');
<% else %>
$(window).off('scroll');
$('.pagination').remove();
<% end %>
